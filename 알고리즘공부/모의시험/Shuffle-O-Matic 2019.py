# Shuffle-O-Matic 2019


def shuffle(arr, S):

    left = len(arr)/2
    right = len(arr)/2 + 1
    order = range(len(arr))
    N = len(arr)
    for i in range(len(arr)):







T = int(input())
for test_case in range(1, T + 1):

    print("#%d" % test_case)